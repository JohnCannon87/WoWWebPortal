plugins {
    java
}

allprojects {
    // Set common group and version
    group = "com.github.johncannon87.WoWWebPortal"
    version = "1.0.0"
    // Configure Java 21 for all subprojects
    tasks.withType<JavaCompile> {
    options.release.set(21)
    }
}